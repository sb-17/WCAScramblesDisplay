node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.vercel

.env
.env.local

*.zip

certificates

# Copied from pdfjs-dist on predev/prebuild, so it stays in step with the installed version.
public/pdf.worker.min.mjs
